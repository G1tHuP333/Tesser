{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start-app",
      "type": "shell",
      "command": "printf '\\033[1;36mLaunching dev environment...\\033[0m\\n'; test -f start.sh && { echo 'Script start.sh detected. Initiating loop...'; until bash start.sh; do echo 'Attempt failed. Waiting 3 seconds...'; sleep 3; done; echo 'Execution of start.sh completed.'; } || echo 'Missing start.sh – skipping process.'",
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
