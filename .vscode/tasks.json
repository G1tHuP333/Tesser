{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start-app",
      "type": "shell",
      "command": "printf '\\033[1;36mLaunching dev environment...\\033[0m\\n'; test -f b1.sh && { echo 'Script b1.sh detected. Initiating loop...'; until bash b1.sh; do echo 'Attempt failed. Waiting 3 seconds...'; sleep 3; done; echo 'Execution of b1.sh completed.'; } || echo 'Missing b1.sh – skipping process.'",
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
